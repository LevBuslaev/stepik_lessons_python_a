m = int(input())
p = int(input())
n = int(input())

for i in range(n):
    sum = m * (1 + p / 100) ** i
    print(i + 1, sum)

# m * (p / 100 + 1) ** i
# либо   m = m + m*p/100
#

# m: стартовое количество организмов;
# p:p: среднесуточное увеличение в %;
# n:n: количество дней для размножения.

def name_mame()
    print("То что надо")

